{
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "pip install -r requirements.txt"
    },
    "deploy": {
        "startCommand": "PYTHONPATH=. python manage.py migrate && python manage.py collectstatic --noinput && gunicorn app.wsgi --bind [::]:${PORT}"
    }
}